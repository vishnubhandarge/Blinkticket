import { Routes } from '@angular/router';
import { SeeAllCardsComponent } from '../components/see-all-cards/see-all-cards.component';

export const routes: Routes = [
    {
        path: 'see all cards',
        component: SeeAllCardsComponent
    }
];
